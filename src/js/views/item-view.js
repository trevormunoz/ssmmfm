/*global define*/

define ([
    'backbone'
], function(Backbone) {
    'use strict';

   var ItemView = Backbone.View.extend ({
        tagName: 'div',
        className: 'viewer',
        
        });

return ItemView;

});